# CMSIS-DAP programmer (Formerly Atmel EDBG programmer)

This is a simple command line utility for programming ARM-based MCUs
(currently only Atmel) through CMSIS-DAP SWD interface. 

This version is designed to work on Linux host by accessing sysfs GPIOs for
SWDIO and SWCLK.
